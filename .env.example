# Vite exposes env vars prefixed with VITE_ to your app.
# Copy this file to .env and fill in values. Never commit .env.

# Example (for Map page if you use Google Maps):
# VITE_GOOGLE_MAPS_API_KEY=your_key_here
